# react-scripts 5 declares peer deps on React <=18, so a strict peer
# resolution refuses this tree (React 19) and pulls in optional peers that the
# lock file does not carry. The lock is generated with this setting on, so CI
# has to install with it too or `npm ci` reports the lock as out of sync.
legacy-peer-deps=true
